# 🔥 First Dynamics Widget

Интерактивный виджет для отображения Streaks и Achievements из Notion с продвинутыми анимациями.

![Widget Preview](./preview.png)

## ✨ Возможности

- 🔥 **Анимированные Streak карточки** с огнём, частицами и конфетти
- 🏆 **Achievement бейджи** с эффектами свечения и sparkle анимациями
- 📊 **Статусы систем** в реальном времени из Notion
- 🎨 **Glassmorphism дизайн** с неоновыми эффектами
- 🔄 **Автообновление** данных из Notion API
- 📱 **Адаптивный дизайн** для мобильных устройств

---

## 📋 Пошаговая инструкция по установке

### Шаг 1: Подготовка Notion Integration

1. **Создай Notion Integration:**
   - Перейди на https://www.notion.so/my-integrations
   - Нажми "New integration"
   - Заполни:
     - Name: `First Dynamics Widget`
     - Associated workspace: Твой workspace
   - Нажми "Submit"
   - **Скопируй "Internal Integration Token"** (начинается с `secret_`)

2. **Подключи Integration к базам данных:**
   - Открой каждую из баз данных в Notion:
     - 📊 Статусы Систем 1Д
     - 🏅 Streak Achievements  
     - 🏆 Ежедневный Трекер 1Д
   - В каждой базе нажми `...` → `Add connections` → Выбери `First Dynamics Widget`

3. **Получи ID баз данных:**
   - Открой базу данных в браузере
   - URL выглядит так: `https://notion.so/workspace/DATABASE_ID?v=...`
   - Скопируй `DATABASE_ID` (32 символа между `/` и `?`)

---

### Шаг 2: Создание Cloudflare Worker (Прокси)

Notion API не позволяет прямые запросы из браузера (CORS), поэтому нужен прокси.

1. **Зарегистрируйся на Cloudflare:**
   - Перейди на https://workers.cloudflare.com/
   - Создай бесплатный аккаунт

2. **Создай новый Worker:**
   - Зайди в Dashboard → Workers & Pages
   - Нажми "Create application" → "Create Worker"
   - Дай имя, например: `notion-proxy`
   - Нажми "Deploy"

3. **Редактируй код Worker:**
   - Нажми "Edit code"
   - Удали весь код и вставь содержимое файла `cloudflare-worker.js`
   - **Замени `YOUR_NOTION_INTEGRATION_TOKEN`** на твой токен
   - **Добавь свой GitHub Pages URL** в `ALLOWED_ORIGINS`
   - Нажми "Save and deploy"

4. **Скопируй URL Worker:**
   - URL будет выглядеть так: `https://notion-proxy.YOUR_SUBDOMAIN.workers.dev`
   - Сохрани его, он понадобится позже

---

### Шаг 3: Настройка проекта

1. **Скачай проект:**
   ```bash
   # Создай папку и перейди в неё
   mkdir first-dynamics-widget
   cd first-dynamics-widget
   
   # Скопируй все файлы проекта сюда
   ```

2. **Установи зависимости:**
   ```bash
   npm install
   ```

3. **Настрой конфигурацию:**
   
   Открой файл `src/config.js` и заполни:

   ```javascript
   export const CONFIG = {
     // Твой Notion Integration Token
     NOTION_TOKEN: 'secret_xxxxxxxxxxxxx',
     
     // ID твоих баз данных
     SYSTEMS_DB_ID: 'xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx',
     ACHIEVEMENTS_DB_ID: 'xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx',
     DAILY_TRACKER_DB_ID: 'xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx',
     
     // ... остальные настройки
   };

   // URL твоего Cloudflare Worker
   export const NOTION_PROXY_URL = 'https://notion-proxy.YOUR_SUBDOMAIN.workers.dev';
   ```

4. **Настрой vite.config.js:**
   
   Замени `base` на имя твоего репозитория:
   ```javascript
   export default defineConfig({
     base: '/first-dynamics-widget/', // Имя твоего репозитория
     // ...
   });
   ```

5. **Протестируй локально:**
   ```bash
   npm run dev
   ```
   
   Открой http://localhost:5173 в браузере

---

### Шаг 4: Публикация на GitHub Pages

1. **Создай репозиторий на GitHub:**
   - Перейди на https://github.com/new
   - Имя репозитория: `first-dynamics-widget`
   - Сделай его Public
   - Нажми "Create repository"

2. **Загрузи код:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/first-dynamics-widget.git
   git push -u origin main
   ```

3. **Собери проект:**
   ```bash
   npm run build
   ```

4. **Опубликуй на GitHub Pages:**
   ```bash
   npm run deploy
   ```

5. **Включи GitHub Pages:**
   - Перейди в Settings репозитория → Pages
   - Source: "Deploy from a branch"
   - Branch: `gh-pages` / `/ (root)`
   - Нажми Save

6. **Подожди 2-3 минуты** и открой:
   ```
   https://YOUR_USERNAME.github.io/first-dynamics-widget/
   ```

---

### Шаг 5: Встраивание в Notion

1. **Открой свой Dashboard в Notion**

2. **Добавь блок Embed:**
   - Напиши `/embed` и выбери "Embed"
   - Вставь URL виджета:
     ```
     https://YOUR_USERNAME.github.io/first-dynamics-widget/
     ```

3. **Настрой размер:**
   - Растяни блок до нужного размера
   - Рекомендуемая ширина: Full width
   - Рекомендуемая высота: 600-800px

---

## 🔧 Настройка

### Интервал обновления

В `src/config.js`:
```javascript
REFRESH_INTERVAL: 60000, // 1 минута (в миллисекундах)
```

### Отключение анимаций

```javascript
ENABLE_ANIMATIONS: false,
ENABLE_PARTICLES: false,
```

### Изменение цветов категорий

В `src/config.js` измени `STREAK_CATEGORIES`:
```javascript
'Deep Work': {
  gradient: 'from-blue-500 to-cyan-500',
  bgGradient: 'from-blue-500/20 to-cyan-500/10',
  // ...
},
```

---

## 🐛 Решение проблем

### "CORS error" в консоли

- Убедись, что Cloudflare Worker правильно настроен
- Проверь, что URL GitHub Pages добавлен в `ALLOWED_ORIGINS`

### "Notion API error: 401"

- Проверь правильность `NOTION_TOKEN`
- Убедись, что Integration подключена к базам данных

### "Notion API error: 404"

- Проверь правильность ID баз данных
- Убедись, что Integration имеет доступ к базам

### Данные не обновляются

- Проверь консоль браузера на ошибки
- Убедись, что `REFRESH_INTERVAL` установлен

### Виджет показывает "Demo Mode"

- Это значит, что подключение к Notion не удалось
- Проверь все настройки выше

---

## 📁 Структура проекта

```
first-dynamics-widget/
├── src/
│   ├── App.jsx          # Главный компонент
│   ├── components.jsx   # Анимированные компоненты
│   ├── config.js        # Настройки (РЕДАКТИРОВАТЬ!)
│   ├── notionService.js # API для Notion
│   ├── main.jsx         # Точка входа
│   └── index.css        # Стили
├── cloudflare-worker.js # Код прокси для Cloudflare
├── index.html
├── package.json
├── vite.config.js
├── tailwind.config.js
├── postcss.config.js
└── README.md
```

---

## 🚀 Обновление виджета

После изменений в коде:

```bash
npm run build
npm run deploy
```

Изменения появятся через 1-2 минуты.

---

## 💡 Дополнительные идеи

- [ ] Добавить звуковые эффекты при получении Achievement
- [ ] Темная/светлая тема
- [ ] Экспорт статистики
- [ ] Уведомления о новых достижениях
- [ ] Интеграция с календарём

---

## 📄 Лицензия

MIT License - используй как хочешь! 🎉

---

## 🙋 Поддержка

Если есть вопросы:
1. Проверь раздел "Решение проблем"
2. Открой Issue на GitHub
3. Напиши в комментариях

---

Сделано с ❤️ для First Dynamics
